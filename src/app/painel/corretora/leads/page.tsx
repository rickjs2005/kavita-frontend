// src/app/painel/corretora/leads/page.tsx
import LeadsClient from "./LeadsClient";

export const metadata = {
  title: "Leads | Painel da Corretora",
};

export default function LeadsPage() {
  return <LeadsClient />;
}
