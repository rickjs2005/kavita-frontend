// src/utils/imageUrl.ts
// Re-exports absUrl as resolveImageUrl for backward compatibility.
// All new code should import absUrl directly from "@/utils/absUrl".
export { absUrl as resolveImageUrl } from "@/utils/absUrl";
